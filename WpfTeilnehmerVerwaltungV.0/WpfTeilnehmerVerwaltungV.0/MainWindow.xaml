<Window x:Class="WpfTeilnehmerVerwaltung.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTeilnehmerVerwaltung"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="MaxWidth" Value="250"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Titel -->
        <Label Content="Registrieren" Grid.Row="0" Grid.Column="0"
               FontWeight="Bold" FontSize="20" Foreground="SteelBlue" Margin="0,0,0,20"/>

        <!-- Anrede -->
        <Label x:Name="LabelAnrede" Content="Anrede" Grid.Row="1" Grid.Column="0"/>
        <ComboBox x:Name="ComboBoxAnrede" Grid.Row="1" Grid.Column="2">
            <ComboBoxItem Content="Herr"/>
            <ComboBoxItem Content="Frau"/>
        </ComboBox>

        <!-- Name -->
        <Label Content="Name" Grid.Row="2" Grid.Column="0"/>
        <TextBox x:Name="TextBoxName" Grid.Row="2" Grid.Column="2"/>

        <!-- Vorname -->
        <Label Content="Vorname" Grid.Row="3" Grid.Column="0"/>
        <TextBox x:Name="TextBoxVorname" Grid.Row="3" Grid.Column="2"/>

        <!-- Postleitzahl -->
        <Label Content="Postleitzahl" Grid.Row="4" Grid.Column="0"/>
        <TextBox x:Name="TextBoxPlz" Grid.Row="4" Grid.Column="2"/>

        <!-- Kurs -->
        <Label Content="Kurs" Grid.Row="5" Grid.Column="0"/>
        <ComboBox x:Name="ComboBoxKurs" Grid.Row="5" Grid.Column="2">
            <ComboBoxItem Content="C#"/>
            <ComboBoxItem Content="Java"/>
            <ComboBoxItem Content="Python"/>
            <ComboBoxItem Content="Webentwicklung"/>
        </ComboBox>

        <!-- Vorsteuerabzug -->
        <Label Content="Vorsteuerabzug" Grid.Row="6" Grid.Column="0" VerticalAlignment="Top"/>
        <StackPanel Grid.Row="6" Grid.Column="2" VerticalAlignment="Top">
            <RadioButton x:Name="RadioBerechtigt" Content="berechtigt" GroupName="Vorsteuerabzug" Margin="0,5"/>
            <RadioButton x:Name="RadioNichtBerechtigt" Content="nicht berechtigt" GroupName="Vorsteuerabzug" Margin="0,5"/>
            <RadioButton x:Name="RadioTeilweise" Content="teilweise" GroupName="Vorsteuerabzug" Margin="0,5"/>
        </StackPanel>

        <!-- Buttons -->
        <Button x:Name="ButtonHinzufuegen" Content="Hinzufügen" Grid.Row="7" Grid.Column="2"
                HorizontalAlignment="Right" Click="ButtonHinzufuegen_Click"/>
        <Button x:Name="ButtonAbbrechen" Content="Abbrechen" Grid.Row="7" Grid.Column="3"
                HorizontalAlignment="Right" Click="ButtonAbbrechen_Click"/>

    </Grid>
</Window>